# -*- coding: utf-8 -*-
"""
DEPRECATED: This file is kept for backward compatibility only.
All models have been moved to the `models/` package.

Import directly from api.models (which now loads from the package).
The models folder is automatically detected by Django as the models module.

For new code, prefer explicit imports:
    from api.models import Produit, Facture, Client

Or import specific modules:
    from api.models.products import Produit
    from api.models.billing import Facture
"""

# This file can be safely deleted after confirming all imports work.
# Django will use the models/ package instead.
